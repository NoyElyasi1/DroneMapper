#include "PositionSensorMock.hpp"
#include <cmath>

// All logic is inline in PositionSensorMock.hpp.
// This translation unit exists so the build system links correctly
// and to provide a place for future non-trivial expansions
// (e.g. adding sensor noise or drift simulation).

namespace dm {
} // namespace dm
