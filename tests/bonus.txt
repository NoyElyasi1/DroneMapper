Bonus Additions
===============

1. Automatic Testing with GTest
--------------------------------
External Library: Google Test (GTest) v1.14.0
  Repository : https://github.com/google/googletest
  License    : BSD 3-Clause
  Approval   : Received forum approval for use in the bonus test suite only.
               Fetched automatically via CMake FetchContent; NOT linked into
               the main drone_mapper executable.

Usage:
  - TEST(Suite, Name) macros define isolated test cases across seven test files.
  - EXPECT_EQ / EXPECT_TRUE / EXPECT_NEAR / EXPECT_THROW macros assert
    correctness; non-fatal so all failures in a test are reported together.
  - GTest::gtest_main provides main(); gtest_discover_tests() registers each
    binary with CTest for "ctest --output-on-failure" execution.

The project includes a comprehensive automated test suite located in tests/.

Test files:
  - tests/test_config_parser.cpp  — 17 tests for DroneConfig, MissionConfig, toGrid()
  - tests/test_sparse_map.cpp     — 17 tests for SparseBuildingMap (cell access, file I/O, scoring)
  - tests/test_mocks.cpp          — 25 tests for PositionSensorMock, MovementDriverMock, LidarMock
  - tests/test_drone.cpp          —  7 tests for the Drone algorithm (unit + integration)
  - tests/test_dense_map.cpp      — 16 tests for SparseBuildingMap in dense mode (the real sim path)
  - tests/test_simulator.cpp      —  8 tests for Simulator end-to-end (run, output, error handling)
  - tests/test_boundaries.cpp     —  8 tests for mission boundary enforcement in MovementDriverMock

Total: 98 automated tests, all passing.

How to run:
  mkdir build && cd build
  cmake .. -DCMAKE_BUILD_TYPE=Release
  make -j4
  ctest --output-on-failure

Coverage summary:
  - Config parsing: valid files, missing keys, bad values, inline comments,
    file not found, inverted lidar range, FOVC clamping, Windows line endings,
    partial/missing start position, negative resolution.
  - SparseBuildingMap (sparse mode): get/set/overwrite cells, negative indices,
    file roundtrip, bad/comment lines, invalid path, score formula correctness.
  - SparseBuildingMap (dense mode): initDense, cell access, out-of-bounds safety,
    mappedCount correctness, loadFromFile into dense array, saveToFile roundtrip,
    calculateScore with dense ground truth, LidarMock ray-casting against dense map.
  - Mocks: position and angle normalisation, rotate/advance/elevate with collision
    detection, lidar beam count, unit vector validation, Zmin/Zmax behaviour.
  - Movement boundaries: advance and elevate rejected when they would exceed
    minX/maxX/minY/maxY/minHeight/maxHeight of the mission volume.
  - Drone: first step marks cell empty, wall detection, beam path marking,
    termination in enclosed room, obstacle everywhere, integration score > threshold.
  - Simulator (end-to-end): valid room returns 0 and writes map_output.txt,
    output map is non-empty, no errors file on clean run, missing map file returns
    non-zero, start inside wall returns non-zero, bad drone config uses defaults.
